{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build and Collect Bots",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/build_bots.sh",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "runtimeExecutable": "/bin/bash"
        }
    ]
}